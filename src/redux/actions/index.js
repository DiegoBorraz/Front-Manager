import { types } from "./types";

export const actions = {
  increment: {
    type: types.INCREMENT
  },
  incrementAsync: {
    type: types.INCREMENT_ASYNC
  },
  testeAsync: {
    type: types.TESTE
  }
};
